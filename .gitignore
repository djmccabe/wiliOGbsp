build/
build-tests/
build-nodiag/
*.uf2
*.elf
*.bin
# ...but the FPGA bitstream is a checked-in source artifact, not a build output.
!bsp/main_cpu/fpga/fpga_default_v5.bin
*.hex
*.map
__pycache__/
.venv/
.idea/
.vs/
.superpowers/
.claude/worktrees/
.claude/settings.local.json

# Maintainer-only orientation doc. AGENTS.md is the published one.
AGENTS.local.md

# NOTE: docs/superpowers/ and most of docs/hardware/ are intentionally still
# TRACKED here -- this repository is private and they are its dev history.
# They are kept out of the public repository by tools/publish.py, which builds
# it from an explicit ALLOWLIST. Adding a new internal doc requires no
# gitignore change; it simply will not be published unless it is listed there.
